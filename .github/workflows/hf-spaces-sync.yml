name: Sync to Hugging Face Spaces

on:
  push:
    branches:
      - main
    paths:
      - 'hf_spaces/**'
      - 'src/kiara/**'
      - '.github/workflows/hf-spaces-sync.yml'
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install and Verify Hugging Face CLI
        run: |
          python -m pip install --upgrade pip
          pip install "huggingface_hub[cli]"
          
          # Debug: Check Python and pip configuration
          echo "Python version: $(python --version)"
          echo "Pip version: $(pip --version)"
          echo "Python executable: $(which python)"
          echo "Pip install location: $(python -m pip show huggingface_hub | grep '^Location:' || echo 'Not found')"
          
          # Check if huggingface-cli is in PATH
          echo "PATH: $PATH"
          if command -v huggingface-cli &> /dev/null; then
            echo "✅ huggingface-cli found in PATH: $(which huggingface-cli)"
            huggingface-cli --version
          else
            echo "⚠️ huggingface-cli not found in PATH, checking Python module..."
            python -m huggingface_hub.commands.huggingface_cli --version || echo "❌ Failed to run via Python module"
          fi
          
          # Ensure user site-packages bin is in PATH for subsequent steps
          # Note: This PATH modification only affects subsequent workflow steps, not this one
          echo "$(python -m site --user-base)/bin" >> $GITHUB_PATH

      - name: Sync to Hugging Face Spaces
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_USERNAME: ${{ vars.HF_USERNAME || 'nexageapps' }}
          HF_SPACE_NAME: ${{ vars.HF_SPACE_NAME || 'kiara-slm-project' }}
        run: |
          # Configure git
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

          # Login to Hugging Face (enables git credential helper for push)
          if command -v huggingface-cli &> /dev/null; then
            huggingface-cli login --token $HF_TOKEN
          else
            echo "Using Python module invocation for huggingface-cli"
            python -m huggingface_hub.commands.huggingface_cli login --token $HF_TOKEN
          fi

          # Clone with auth so push works (HTTPS + token)
          SPACE_REPO="https://${HF_USERNAME}:${HF_TOKEN}@huggingface.co/spaces/${HF_USERNAME}/${HF_SPACE_NAME}"
          echo "Syncing to: $SPACE_REPO"

          # Create a temporary directory for the Space
          TEMP_DIR=$(mktemp -d)
          cd $TEMP_DIR

          # Clone the Space (create if doesn't exist)
          git clone $SPACE_REPO space || {
            echo "Space doesn't exist, creating new Space..."
            if command -v huggingface-cli &> /dev/null; then
              huggingface-cli repo create --type space --space-sdk gradio "${HF_USERNAME}/${HF_SPACE_NAME}"
            else
              echo "Using Python module invocation for huggingface-cli"
              python -m huggingface_hub.commands.huggingface_cli repo create --type space --space-sdk gradio "${HF_USERNAME}/${HF_SPACE_NAME}"
            fi
            git clone $SPACE_REPO space
          }

          cd space

          # Copy files from repository
          echo "Copying files..."

          # Copy app.py and requirements.txt to root
          cp $GITHUB_WORKSPACE/hf_spaces/app.py .
          cp $GITHUB_WORKSPACE/hf_spaces/requirements.txt .

          # Copy README
          cp $GITHUB_WORKSPACE/hf_spaces/README.md .

          # Copy source code
          rm -rf src
          cp -r $GITHUB_WORKSPACE/src .

          # Create a .gitignore if it doesn't exist
          if [ ! -f .gitignore ]; then
            echo "__pycache__/" > .gitignore
            echo "*.py[cod]" >> .gitignore
            echo "*\$py.class" >> .gitignore
            echo "*.so" >> .gitignore
            echo ".Python" >> .gitignore
            echo "*.egg-info/" >> .gitignore
            echo ".env" >> .gitignore
            echo ".venv" >> .gitignore
            echo "venv/" >> .gitignore
            echo "checkpoints/" >> .gitignore
            echo "*.pt" >> .gitignore
            echo "*.pth" >> .gitignore
            echo ".DS_Store" >> .gitignore
          fi

          # Check if there are changes
          if git diff --quiet && git diff --cached --quiet; then
            echo "No changes to sync"
            exit 0
          fi

          # Commit and push
          git add .
          git commit -m "Sync from GitHub: $GITHUB_SHA" -m "Auto-sync triggered by push to main branch"
          git push

          echo "✅ Successfully synced to Hugging Face Spaces"
          echo "🌐 View your Space at: https://huggingface.co/spaces/${HF_USERNAME}/${HF_SPACE_NAME}"

      - name: Comment on commit (optional)
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HF_USERNAME: ${{ vars.HF_USERNAME || 'nexageapps' }}
          HF_SPACE_NAME: ${{ vars.HF_SPACE_NAME || 'kiara-slm-project' }}
        run: |
          # Create a comment on the commit with the Space URL
          SPACE_URL="https://huggingface.co/spaces/${HF_USERNAME}/${HF_SPACE_NAME}"
          echo "Deployed to Hugging Face Spaces: $SPACE_URL"
